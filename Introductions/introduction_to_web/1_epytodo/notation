 Note : 11
preliminaries (2 / 2) :

Web Server (4 / 5) :
The files are well organized (see the example tree on the subject)

.env file, the file exists and contains the variables listed below:
  - MYSQL_DATABASE
  - MYSQL_HOST
  - MYSQL_USER
  - MYSQL_ROOT_PASSWORD
  - SECRET //used for the JSON Web Token (JWT)
Routes (does it exists) (0 / 3) :

Routes (Is it well done) (0 / 3) :

Password (0 / 1) :

Token (0 / 3) :

SQL DB (5 / 5) :
The database contains at least 2 tables (user + Todo).
Fields have the correct type.
Todo table contains a foreign key 'user_id'.
Each table has an auto-incremented ID.
